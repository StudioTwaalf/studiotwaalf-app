/**
 * Editor layout — intentionally bare (no nav/footer).
 * The CanvasEditor component manages its own full-screen layout.
 */
export default function EditorLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
